jQuery(document).ready(function(){
	jQuery('#croptooltip').tooltip({
		content : "If this is set to true, the image will be cropped to these exact dimensions. Otherwise the image will be resized as close as it can be to these dimensions."
	});
});
